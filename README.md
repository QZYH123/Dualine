# Prose ↔ Code reader (working title)

A tool for reading a project after vibe coding: prose that stays tied to real code, in a layout built for paired reading.

Design-first. Frontend by Fable; backend via Grok subagent.
